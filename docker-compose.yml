version: '3.1'

services:
  db:
    image: mysql:latest
    container_name: local-mysql-fast-campus
    ports:
      - "3306:3306"
    environment:
      MYSQL_ROOT_PASSWORD: superSecretPassword
      MYSQL_DATABASE: simple-forum
    volumes:
      - /Users/fiqry/go/Simple-Forum/simple-forum_mysql/db:/var/lib/mysql